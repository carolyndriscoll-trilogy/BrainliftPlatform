Table Header Fix
Looking at your screenshot, here are the problems with the blue header:
IssueProblemColumn widthsHeaders don't align with content belowText alignment"Correctness (1-5)" is centered, others are leftPaddingInconsistent spacing"Fact (as written)"Too narrow for the content it holds

Quick Fix CSS
css/* ========================================
   FACT GRADING TABLE HEADER FIX
   ======================================== */

/* Table Container */
.fact-table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
  border-radius: 12px;
  overflow: hidden;
}

/* Header Row */
.fact-table thead tr {
  background: #1E3A5F;
}

.fact-table th {
  color: white;
  font-size: 13px;
  font-weight: 600;
  text-align: left;
  padding: 16px 20px;
  white-space: nowrap;
}

/* Fixed Column Widths - KEY FIX */
.fact-table th:nth-child(1),
.fact-table td:nth-child(1) {
  width: 80px;          /* Fact ID */
  text-align: center;
}

.fact-table th:nth-child(2),
.fact-table td:nth-child(2) {
  width: 35%;           /* Fact (as written) */
  text-align: left;
}

.fact-table th:nth-child(3),
.fact-table td:nth-child(3) {
  width: 120px;         /* Correctness */
  text-align: center;
}

.fact-table th:nth-child(4),
.fact-table td:nth-child(4) {
  width: auto;          /* Verification Notes - takes remaining space */
  text-align: left;
}

/* Body Rows */
.fact-table td {
  padding: 16px 20px;
  border-bottom: 1px solid #E5E7EB;
  vertical-align: top;
  font-size: 14px;
  color: #374151;
}

.fact-table tbody tr:hover {
  background: #F9FAFB;
}

/* Fact ID styling */
.fact-table td:nth-child(1) {
  font-weight: 600;
  color: #1E3A5F;
  font-family: monospace;
}

If Using Flexbox/Grid Instead of Table
If your layout isn't a <table>, here's the fix:
css/* Header Row as Flex */
.table-header {
  display: grid;
  grid-template-columns: 80px 1fr 120px 1.5fr;
  background: #1E3A5F;
  border-radius: 12px 12px 0 0;
  padding: 0;
}

.table-header-cell {
  color: white;
  font-size: 13px;
  font-weight: 600;
  padding: 16px 20px;
}

/* Center the Correctness column */
.table-header-cell:nth-child(3) {
  text-align: center;
}

/* Content Rows - same grid */
.table-row {
  display: grid;
  grid-template-columns: 80px 1fr 120px 1.5fr;
  border-bottom: 1px solid #E5E7EB;
}

.table-cell {
  padding: 16px 20px;
  font-size: 14px;
  color: #374151;
}

.table-cell:nth-child(3) {
  text-align: center;
}
```

---

## Replit AI Prompt

Paste this:
```
Fix the fact grading table header alignment:

1. Set fixed column widths:
   - Fact ID: 80px, centered
   - Fact (as written): 35% of remaining space, left-aligned
   - Correctness (1-5): 120px, centered
   - Verification Notes: remaining space, left-aligned

2. Make sure header columns and body columns use the SAME widths

3. Use consistent padding: 16px vertical, 20px horizontal

4. Keep the navy background (#1E3A5F) and white text

5. Add vertical-align: top to all table cells so long text aligns properly

6. The grid-template-columns or column widths must match between thead and tbody

Visual Result
BeforeAfterColumns misalignedPerfect alignment header ↔ contentInconsistent centeringFact ID + Correctness centered, others leftCramped paddingConsistent 16px/20px spacing