Current Issues I See
IssueProblem"NOT GRADEABLE" badge cut offOverlaps with title "Direct Instruction Desig..."Card heights unevenCards don't match heights in the same rowNot gradeable card same styleShould be visually dimmed/differentNo score on 0 facts cardLooks incomplete without the circleTitle truncation"Direct Instruction Design Manual" gets cutBadge position inconsistentStatus badge crowds the title

Quick Fixes
Fix 1: Badge Position & Title Truncation
css/* Card needs more padding-right for badge */
.brainlift-card {
  position: relative;
  padding: 20px;
  padding-right: 24px;
}

/* Status badge - position it better */
.card-status {
  position: absolute;
  top: 16px;
  right: 16px;
  padding: 4px 10px;
  border-radius: 6px;
  font-size: 10px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.3px;
  white-space: nowrap;
  z-index: 1;
}

/* Title area needs space for badge */
.card-header {
  padding-right: 100px; /* Space for badge */
}

.card-title {
  font-size: 17px;
  font-weight: 600;
  color: #111827;
  margin: 0 0 6px 0;
  line-height: 1.3;
  /* Allow wrapping, don't truncate */
  word-wrap: break-word;
}

Fix 2: Equal Height Cards
css/* Grid with equal height cards */
.brainlift-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 20px;
  align-items: stretch; /* Key for equal heights */
}

/* Card as flex column */
.brainlift-card {
  display: flex;
  flex-direction: column;
  height: 100%; /* Fill grid cell */
}

/* Push stats to bottom */
.card-stats {
  margin-top: auto;
  padding-top: 16px;
  border-top: 1px solid #F3F4F6;
}

Fix 3: Not Gradeable Card Styling
css/* Dimmed state for not gradeable */
.brainlift-card.not-gradeable {
  background: #F9FAFB;
  opacity: 0.7;
  border: 1px dashed #D1D5DB;
}

.brainlift-card.not-gradeable:hover {
  opacity: 0.85;
  border-color: #9CA3AF;
  transform: none;
  box-shadow: none;
}

/* Red badge */
.card-status.not-gradeable {
  background: #FEF2F2;
  color: #DC2626;
  border: 1px solid #FECACA;
}

/* Red facts badge for 0 */
.stat-badge.facts.zero {
  background: #FEF2F2;
  color: #DC2626;
}

Fix 4: Show "N/A" for Missing Score
css/* Score placeholder for not gradeable */
.score-circle.na {
  background: #E5E7EB;
  color: #6B7280;
  font-size: 12px;
}
html<!-- For not gradeable card, show N/A -->
<div class="score-preview">
  <div class="score-label">Mean<br>Score</div>
  <div class="score-circle na">N/A</div>
</div>
```

---

## Full Replit AI Prompt
```
Fix the brainlift cards layout issues:

1. BADGE POSITION:
   - Add padding-right: 100px to .card-header so title doesn't overlap badge
   - Badge should be position: absolute, top: 16px, right: 16px
   - Badge font-size: 10px with white-space: nowrap

2. EQUAL HEIGHT CARDS:
   - Grid should use align-items: stretch
   - Cards should be display: flex, flex-direction: column, height: 100%
   - .card-stats should have margin-top: auto to push to bottom

3. NOT GRADEABLE CARD:
   - Add class .not-gradeable to the card
   - Background: #F9FAFB (light gray)
   - Opacity: 0.7
   - Border: 1px dashed #D1D5DB
   - Muted hover (no lift, no shadow)

4. ZERO FACTS BADGE:
   - When facts = 0, add class .zero to .stat-badge.facts
   - Style: red background #FEF2F2, red text #DC2626

5. N/A SCORE:
   - For not gradeable cards, show score circle with "N/A"
   - Gray background #E5E7EB, gray text #6B7280

6. TITLE WRAPPING:
   - Don't truncate titles, let them wrap to 2 lines if needed
   - Use word-wrap: break-word on .card-title

Visual Comparison
BeforeAfterBadge overlaps titleBadge in its own spaceUneven card heightsAll cards same height in rowNot gradeable looks sameDimmed with dashed border0 facts looks normalRed badge, "N/A" scoreTitle cut offFull title wraps properly

